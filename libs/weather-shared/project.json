{
  "root": "libs/weather-shared",
  "projectType": "library",
  "sourceRoot": "libs/weather-shared",
  "targets": {
    "build": {
      "executor": "@nx-dotnet/core:build",
      "outputs": ["dist/libs/weather-shared"],
      "options": {
        "configuration": "Debug",
        "noDependencies": true
      },
      "configurations": {
        "production": {
          "configuration": "Release"
        }
      }
    },
    "lint": {
      "executor": "@nx-dotnet/core:format"
    }
  },
  "tags": ["nx-dotnet"]
}
